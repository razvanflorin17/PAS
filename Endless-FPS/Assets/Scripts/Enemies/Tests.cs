using System;
using System.Collections;
using System.Collections.Generic;
using System.Diagnostics;
using UnityEngine;
/*
public class Tests : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        EnemyBehaviour enemy = new EnemyBehaviour();

        enemy.health = 0;

        enemy.update();

        if (enemy)
            UnityEngine.Debug.Log("Test 1 passed");
        else
            UnityEngine.Debug.Log("Test 1 failed");


        EnemyBehaviour enemy = new EnemyBehaviour();

        enemy.SearchWalkPoint();

        if(enemy.walkPointSet == true)
            UnityEngine.Debug.Log("Test 2 passed");
        else
            UnityEngine.Debug.Log("Test 2 failed");

        EnemyBehaviour enemy = new EnemyBehaviour();

        enemy.DestroyEnemy();

        if (enemy)
            UnityEngine.Debug.Log("Test 3 passed");
        else
            UnityEngine.Debug.Log("Test 3 failed");

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
*/